#!/usr/bin/env sh
# Studyroom launcher. The real logic is app/start.js — one implementation for every OS.
# `studyroom.cmd` beside this file is its Windows twin.
exec node "$(dirname "$0")/app/start.js" "$@"
